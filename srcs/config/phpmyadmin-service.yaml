apiVersion: v1
kind: Service
metadata:
  name: phpmyadmin
spec:
  ports:
  # - name: http
  #   port: 5050
  #   protocol: TCP
  #   targetPort: 80
  - name: https
    port: 5000
    protocol: TCP
    targetPort: 443
  selector:
    app: phpmyadmin
  type: LoadBalancer